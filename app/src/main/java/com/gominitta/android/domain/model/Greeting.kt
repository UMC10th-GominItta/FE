package com.gominitta.android.domain.model

/**
 * Domain model — framework-agnostic. Pure Kotlin, no Android/data imports.
 * Replace/extend with real domain entities as features are built.
 */
data class Greeting(
    val message: String,
)
